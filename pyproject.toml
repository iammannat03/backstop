[project]
name = "backstop"
version = "0.1.0"
description = "Ticket triage and refund automation, AWS native."
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.35.0",
    "httpx>=0.28.1",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
]
